<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.fragment.favorite.FavoriteFragment">

    <include
        android:id="@+id/topAppBar"
        layout="@layout/appbar_search_bar"/>

    <TextView
        android:id="@+id/tvTitleFavoriteEvents"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/topAppBar"
        android:layout_marginTop="18dp"
        android:layout_marginStart="14dp"
        android:text="@string/favorite_events"
        style="@style/TextBold.TextNormal"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvFavoriteEvents"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/tvTitleFavoriteEvents"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="12dp"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="14dp"
        tools:listitem="@layout/item_list_finished_events"
        tools:itemCount="2"
        android:visibility="visible"/>

</androidx.constraintlayout.widget.ConstraintLayout>